# Dependencies

The installer uses Python packages for the local web server, audio processing, system status, ONNX inference and optional Chatterbox generation.

Install files are stored in the app folder in the release ZIP.
